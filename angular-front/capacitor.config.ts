import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.helloCap',
  appName: 'helloCap',
  webDir: 'dist/angular-front',
};

export default config;
